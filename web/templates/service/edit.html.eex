<h2>Edit service</h2>

<%= render "form.html", changeset: @changeset, doctor: @doctor,
                        action: service_path(@conn, :update, @service) %>

<%= link "Back", to: service_path(@conn, :index, doctor_id: @doctor.id) %>
